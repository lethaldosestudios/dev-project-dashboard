# Server-side GitHub personal access token, used by POST /api/sync/github.
# Local development: also add this to .dev.vars (Wrangler reads that file).
# Production: wrangler secret put GITHUB_TOKEN
GITHUB_TOKEN=
