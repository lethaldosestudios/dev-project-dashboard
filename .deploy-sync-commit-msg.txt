feat(deploys): implement Cloudflare deploy sync

Replaces the no-op /api/sync/deploys placeholder (deleted 2026-09-16) with a real
Cloudflare Worker deployment sync. Vercel stays deferred in TODO.md.

- src/lib/cloudflare.ts: Cloudflare deployments REST client (direct fetch, mirroring
  src/lib/github.ts — no SDK dependency)
- db/migrations/0004_add_deployments.sql + db/schema.sql: new `deployments` table,
  deduped by deployment_id, source column for future providers
- src/app/api/sync/deploys/route.ts: auth-guarded POST (chunked IN() dedupe + db.batch)
  and GET, returning the same SyncResult shape as sync/github
- src/components/cloudflare-deploy-status.tsx: dashboard widget mirroring
  GitHubSyncStatus; wired into src/app/page.tsx
- wrangler.jsonc: CF_ACCOUNT_ID + CF_SCRIPT_NAME vars (CF_API_TOKEN is a secret)
- .dev.vars.example: CF_API_TOKEN + CF_ACCOUNT_ID for local dev
- README.md/AGENTS.md/TODO.md: document the feature and Cloudflare secrets; TODO
  deploy-sync item resolved (Cloudflare done, Vercel deferred)
- docs: re-stamped all state docs to 35dfe26 | 2026-09-23

Verified: tsc --noEmit, next build, jest (26/26), node scripts/docs-check.mjs (8 checks).

Co-authored-by: CommandCodeBot <noreply@commandcode.ai>
