// src/app/capture/page.tsx
export default function CapturePage() {
  return (
    <main className="p-8">
      <h1 className="text-2xl font-semibold mb-4">Quick Capture</h1>
      {/* TODO: prefill from query params (url, title) and save */}
    </main>
  );
}
